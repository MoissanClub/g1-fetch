# PC2 (Jetson Orin NX, JetPack 6.2 / L4T 36.4.3, Python 3.10) — pip packages on top of the `uni` conda env clone.
# torch/torchvision and nvidia-cudss-cu12 are NOT here: the script installs them with --no-deps (Jetson index / PyPI)
# so pip cannot drag CUDA 12.9 pip libraries in next to the system CUDA 12.6.
# Versions verified working on 2026-09-26.
ultralytics==8.4.163
pyrealsense2==2.58.4.10922
onnx==1.23.0
onnxslim==0.1.96
pytest==9.1.1
pytest-timeout==2.4.0
requests==2.34.2
PyYAML==6.0.3
numpy<2                          # 1.26.4 from conda-forge in `uni`; pip must not upgrade it (pinocchio ABI)
